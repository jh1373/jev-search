# 合成コーパスによる候補段の測定（結果）

状態: **測定済み**。ただし**合成データであり、実Vaultや一般的な日本語性能の主張には使わない。**
[プロトコルv1](protocol.md)のpilot/releaseとは別の、RV-02の切り分けを目的とした診断である。

## 目的

RV-02「BM25の候補漏れをJevでは救えない」を、Jevへの投資を増やす前に定量化する。

Jevは**候補に入ったものしか再ランキングできない**。したがって候補段の取りこぼし率が、
そのままJevを含めた製品全体の上限になる。ここを測らずに再ランキングを改善しても、
取りこぼした正解は永久に戻らない。

## 手法

- 生成器: `scripts/gen-corpus.mjs`（seed固定・決定的）。同一seedから同一バイト列を再生成できる。
- 測定器: `scripts/measure-recall.mjs`。**出荷する `src/core/search.ts` を直接import**する。
  再実装ではなく、実際に出荷されるコードを測る。
- 主題24件。各主題に2種類の正解ノートを用意する。
  - grade 2: 事実を述べる規程ノート（`規程/<主題>.md`）
  - grade 1: 同じ主題だが事実を述べない議事録（`議事録/<主題>-検討.md`）
- クエリは各主題2種。**この2種の差が本測定の中心である。**
  - `literal`: ノートの語彙をそのまま使う
  - `synonym`: **ノートの語彙を意図的に避けた**言い換え
- ノイズ: 主題の語彙を共有するが別の話題である distractor に加え、
  日記・レシピ・作業ログ・読書メモ・長文メモ（1200点で分割される規模）を混ぜる。
- 除外ポリシーは適用していない（合成Vaultに `Templates` / `Attachments` が無いため）。
- Recall@50 = 上位50チャンクに正解ノートが含まれる割合（プロトコルv1の定義に合わせた）。

## 結果

seed `20260919`。corpus hashは 400ノートが `175540f7da4cccd4`、10,000ノートが `fbe05e845a31bb2f`（先頭16桁）。

| 指標 | 400ノート | 10,000ノート |
|---|---|---|
| 索引構築 | 281 ms | 7,147 ms |
| クエリ数 | 48 | 48 |
| **Recall@5（全体）** | 62.5% | 60.4% |
| **Recall@10（全体）** | 68.8% | 64.6% |
| **Recall@50（全体）** | 79.2% | **68.8%** |
| MRR（全体） | 0.577 | 0.574 |
| **Recall@50（literal）** | **100.0%** | **91.7%** |
| **Recall@50（synonym）** | **58.3%** | **45.8%** |
| MRR（literal） | 0.899 | 0.889 |
| MRR（synonym） | 0.255 | 0.259 |
| 中央順位（literal / synonym） | 1 / 8 | 1 / 2 |
| 上位50件に入らなかった問 | 10 / 48 | **15 / 48** |

### トークナイザ改善後（現在の出荷コード）

カタカナ語を1語として扱う変更（下の「実験」節）を出荷した後の値。

| 指標 | 400ノート | 10,000ノート |
|---|---|---|
| **Recall@50（全体）** | 79.2% | **70.8%** |
| **Recall@50（literal）** | 100.0% | **95.8%** |
| **Recall@50（synonym）** | 58.3% | 45.8% |
| MRR（全体） | 0.592 | **0.595** |
| MRR（literal） | 0.930 | **0.931** |
| MRR（synonym） | 0.254 | 0.260 |
| 上位50件に入らなかった問 | 10 / 48 | 14 / 48 |
| うち共通語ゼロ | 6 | 6 |
| うち順位が低い | 4（中央値140位） | 8（中央値395位） |
| 索引には存在する（matchedAnywhere） | 87.5% | 87.5% |

## 解釈

1. **語彙が一致すれば候補段はほぼ完璧。** literal の Recall@50 は400ノートで100%、10,000ノートでも91.7%。
   BM25 は「書いた言葉で探す」用途では十分に機能している。
2. **語彙が一致しなければ半分以上を取りこぼす。** synonym の Recall@50 は58.3% / 45.8%。
   つまり**Jevを足しても改善できない問が、全問の41.7%〜54.2%ある**。
3. **規模が増すと悪化する。** literal は100%→91.7%、synonym は58.3%→45.8%へ低下した。
   ノートが増えるほど distractor が上位を占め、正解が50位の外へ押し出される。
   「情報量が増えてもノイズが少ない」という性質は、**現状の候補段には無い**。
4. 1問は**BM25の一致が0件**だった（`パソコンの調子が悪いとき誰に言えばいい`）。
   語彙の重なりが完全に無い問では、候補段は空を返す。
5. MRR は literal 0.89 に対し synonym 0.26。**順位の質も語彙一致に強く依存する。**

## 限界（この結果をどこまで主張できるか）

- **合成データである。** 実際のVaultの語彙分布・表記ゆれ・同義語の使われ方を再現していない。
- **synonymクエリは意図的に厳しく作った。** ノートの語彙を避けるよう設計しており、
  実利用者の検索語がここまで離れているとは限らない。**最悪ケースの測定であって期待値ではない。**
- 主題24件・48問は統計的な結論を出すには少ない。信頼区間も出していない。
- 生成器の作者と評価者が同一である。プロトコルv1が求める独立判定を満たさない。
- したがって**「日本語検索のRecallは45.8%である」とは主張しない。**
  主張できるのは「**語彙一致に依存する候補段では、語彙が離れた問を取りこぼす。その率は規模とともに増える**」という機構である。

## この結果が設計に与える影響

- **ADR-005（BM25先行）は維持する。** 語彙一致の用途では十分に機能しており、置き換えの根拠はない。
- ただし**RV-02の優先度は上がる**。Jev再ランキングの改善は、候補に入った問にしか効かない。
  候補生成の改善（タイトル/aliases、同義語展開、候補多様化、ローカル埋め込み）を
  **Jevの品質測定より先に**比較する価値がある。
- G3（品質検証）でJevの改善を主張する場合、**候補段の上限を併記**する。
  「JevでnDCGが上がった」は候補に入った問の話であり、製品全体の改善ではない。

## 再現方法

```powershell
node scripts/gen-corpus.mjs --out=.sandbox/corpus-10k/vault --notes=10000 --seed=20260919 --clean
node scripts/measure-recall.mjs --vault=.sandbox/corpus-10k/vault --truth=.sandbox/corpus-10k/ground-truth.json --out=.sandbox/corpus-10k/recall.json
```

生成器は決定的なので、同じ `--seed` と `--notes` からは同じ corpus hash が出る。
`.sandbox/` は `.gitignore` 済みで、生成物はリポジトリに入らない。

## 失敗の切り分け

「Recall@50が68.8%」だけでは打ち手が決まらない。カットから漏れた問を原因別に分けた。

| 原因 | 10,000ノート | 意味 |
|---|---|---|
| クエリとノートに**共通する語が1つも無い** | **6件** | BM25の変種では原理的に見つからない。同義語・aliases・埋め込みだけが届く |
| 共通語はあるが**カットより下位** | **9件** | 見つけられる。**スコアリング**の問題 |
| 共通語があるのに索引に無い | 0件 | — |

下位に沈んだ9件の実際の順位は **中央値328位・最悪4,581位**。

### 規模との関係

| | 400ノート | 10,000ノート |
|---|---|---|
| 共通語ゼロで見つからない | 6 | 6 |
| 順位が低くて落ちる | 4（中央値137位） | 9（中央値328位） |
| 索引には存在する（matchedAnywhere） | 87.5% | 87.5% |
| literal の matchedAnywhere | 100% | 100% |

**語彙の壁は規模に依存しない**（どちらも6件＝12.5%）。
一方で**順位の劣化は規模とともに進む**。
つまり規模が増えて悪くなるのは「一致」ではなく「順位」である。

### カットを広げても解決しない

| カット | 全体Recall | literal | synonym |
|---|---|---|---|
| 50 | 68.8% | 91.7% | 45.8% |
| 200 | 72.9% | — | — |
| 1000 | 81.3% | 100.0% | 62.5% |

カットを20倍にしても18.7%が残る。しかも下位の中央値は328位なので、
実用的なカットでは届かない。**候補数を増やすのは打ち手にならない。**

### 示唆

2つの問題は**別の打ち手**を要する。

1. **語彙の壁（12.5%）**: 同義語展開・aliases・ローカル埋め込み。BM25の調整では動かない。
   ただし同義語リストをこの48問に合わせて作れば、それは一般改善ではなく**自作テストへの過学習**になる。
2. **順位の劣化（規模依存）**: スコアリングの改善。規模が増えるほど効いてくる。

### 仮説: カタカナ語の分断

literal で落ちた2件（`デプロイはいつできる` 85位、`障害が起きたらまず何をする` 205位）は、
クエリ語が**対象ノートに実際に含まれている**のに沈んでいる。

bigram化はカタカナ語を分断する。`デプロイ` は `デプ / プロ / ロイ` になり、
このうち `プロ` は `プロジェクト` `プログラマー` などに大量に現れるため、
IDFが下がり無関係なノートにも当たる。**語の同一性が失われている**のが原因という仮説。

### 検証結果

`scripts/experiment-tokenizer.mjs` で、変更前後を同一コーパス・同一クエリで比較した。
`baseline` は変更前（bigramのみ）、`shipped` は出荷後の `tokenize`。

#### 10,000ノート

| | baseline | shipped | 差 |
|---|---|---|---|
| Recall@5 | 60.4% | 62.5% | +2.1 |
| Recall@10 | 64.6% | 66.7% | +2.1 |
| **Recall@50** | 68.8% | 70.8% | **+2.0** |
| **MRR** | 0.574 | 0.595 | **+0.021** |
| **literal Recall@50** | 91.7% | 95.8% | **+4.1** |
| **literal MRR** | 0.889 | 0.931 | **+0.042** |
| synonym Recall@50 | 45.8% | 45.8% | ±0 |
| matchedAnywhere | 87.5% | 87.5% | ±0 |

#### 400ノート

| | baseline | shipped | 差 |
|---|---|---|---|
| Recall@50 | 79.2% | 79.2% | ±0 |
| MRR | 0.577 | 0.592 | +0.015 |
| literal Recall@50 | 100.0% | 100.0% | ±0 |
| **literal MRR** | 0.899 | 0.930 | **+0.031** |

#### 個別の変化（10,000ノート・カット50）

| | 問 | 変化 |
|---|---|---|
| + | `デプロイはいつできる`（literal） | **圏外（実順位85位） → 1位** |
| + | `続けるかどうかを決める締め切り`（synonym） | 8位 → 7位 |
| − | `決められた勉強会を受けないとどうなるか`（synonym） | 14位 → 15位 |

400ノートでも `デプロイはいつできる` は4位 → 1位だった。

#### 判断: 採用した

- 仮説が予測した故障が**両スケールで再現して直った**（85位→1位、4位→1位）。
- literal の MRR が両スケールで改善（+0.042 / +0.031）。語の同一性を回復した効果と整合する。
- `matchedAnywhere` は不変。**一致ではなく順位の問題**だという診断とも整合する。
- 調整可能なパラメータが無く、この48問に合わせ込めない。ビルド時間も増えない。

**主張は限定する:**

- Recall@50 の改善は10,000ノートで+2.0ポイント（48問中1問）にすぎず、400ノートでは0。
  **統計的な改善の証拠とは扱わない。**
- 小さな順位悪化も1〜2件ある（いずれもカット内）。
- 合成コーパス・自作クエリ・評価者＝生成者という限界は変わらない。
- **実文書で再測定するまで「検索精度が上がった」とは宣伝しない。**

## 次の手順

1. 改善候補（タイトル/aliases重み、同義語展開、候補多様化）を**同一コーパス・同一クエリ**で比較する。
2. プロトコルv1のpilot（自作の公開可能な実文書）で同じ測定を行う。
   合成データの数字を実文書へ外挿しない。
3. 候補段を改善した後に、Jev前後のnDCGを測る（G3）。
